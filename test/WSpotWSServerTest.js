/**
 * Test for WSpotWSServer.
 * Runs with mocha.
 */
'use strict'

const WSpotWSServer = require('../lib/WSpotWSServer')
const { ok, equal } = require('assert')

describe('w-spot-w-s-server', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
