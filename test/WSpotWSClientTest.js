/**
 * Test for WSpotWSClient.
 * Runs with mocha.
 */
'use strict'

const WSpotWSClient = require('../lib/WSpotWSClient')
const { ok, equal } = require('assert')

describe('w-spot-w-s-client', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
