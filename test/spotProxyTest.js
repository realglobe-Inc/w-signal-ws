/**
 * Test for spotProxy.
 * Runs with mocha.
 */
'use strict'

const spotProxy = require('../lib/helpers/spotProxy')
const { ok, equal } = require('assert')

describe('spot-proxy', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
