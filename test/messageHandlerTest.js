/**
 * Test for messageHandler.
 * Runs with mocha.
 */
'use strict'

const messageHandler = require('../lib/helpers/messageHandler')
const { ok, equal } = require('assert')

describe('message-handler', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
